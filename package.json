{
  "name": "ai-model-ids",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run src/main.ts",
    "dev": "bun run src/main.ts && bun run dist/index.html",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
